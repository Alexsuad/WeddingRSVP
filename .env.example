# .env.example  # Archivo de ejemplo de variables de entorno (copiar a .env y completar).

# =========================
# BACKEND (FastAPI)
# =========================
DATABASE_URL=sqlite:////ABSOLUTE/PATH/TO/wedding.db   # Ruta absoluta al SQLite (evita bases duplicadas).
SECRET_KEY=CAMBIA_ESTE_SECRETO_LARGO_Y_ALEATORIO       # Clave secreta para firmar JWT.
ACCESS_TOKEN_EXPIRE_MINUTES=360                        # (Opcional) TTL del token, si tu lógica lo usa.

# CORS (los dominios ya están en el código; aquí por referencia)
# WP (producción): https://suarezsiicawedding.com
# RSVP (Streamlit): https://rsvp.suarezsiicawedding.com

# Rate limits (formato: MAX/WINDOW_SECONDS)
LOGIN_RATE_LIMIT=10/900                                # 10 intentos cada 15 min para /api/login.
RECOVER_RATE_LIMIT=5/900                               # 5 intentos cada 15 min para /api/recover-code.

# Evento
EVENT_DATE_HUMAN=22 Mayo 2026                          # Texto amigable para dashboards.
RSVP_DEADLINE=2026-05-10                               # Fecha límite para confirmar (YYYY-MM-DD).
EVENT_TIMEZONE=Europe/Madrid                           # Zona horaria para scheduler (ajusta si es en Rumanía: Europe/Bucharest).

# Email (SendGrid)
SENDGRID_API_KEY=                                      # API Key válida de SendGrid (si se usa envío real).
SENDER_EMAIL=                                         # Dirección remitente (verificada en SendGrid).

# Alertas (opcional)
ALERT_WEBHOOK_URL=                                     # URL webhook (Slack/Teams/Discord) para alertas críticas.

# =========================
# STREAMLIT – Invitados (RSVP)
# =========================
API_BASE_URL=http://127.0.0.1:8000                     # URL del backend FastAPI (cambia a prod en despliegue).
RECOVERY_URL=                                          # (Opcional) URL externa (WP) para “¿Olvidaste tu código?”.

# =========================
# STREAMLIT – Dashboard Admin
# =========================
STREAMLIT_PASSWORD=                                    # Contraseña única para entrar al dashboard admin.

